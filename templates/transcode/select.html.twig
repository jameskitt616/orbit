{% extends 'base.html.twig' %}

{% block body %}
    <div class="max-w-7xl mt-5 justify-between mx-auto flex">
        <div class="w-1/3 p-3 mt-11">
            <div class="rounded border font-semibold p-1 hover:bg-gray-600 cursor-pointer">
                Transcodes
            </div>
        </div>
        <div class="w-2/3 p-4 m-2 border-l">
            <h1>Select video to transcode</h1>
            <div class="max-h-96 overflow-auto">
                {% for video in videos %}
                    <div class="flex justify-between gap-x-6 p-2 my-2 cursor-pointer border rounded hover:bg-gray-600">
                        <div class="flex gap-x-4">
                            <div class="min-w-0 flex-auto">
                                <p class="text-sm font-semibold leading-6">{{ video.fileName }}</p>
                                <p class="truncate text-xs leading-5 text-gray-500">{{ video.getDisplayFilePath }}</p>
                            </div>
                        </div>
                        <div class="hidden sm:flex sm:flex-col sm:items-end">
                            <p class="text-sm leading-6">{{ video.getFileSize }}</p>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
