<div class="w-1/4 p-3 mt-8">
    <div>
        <div class="border-b pb-2 mb-1">
            <a href="{{ path('settings_system_information') }}">
                {{ 'settings.system.information'|trans }}
            </a>
        </div>
        {% if is_granted('ROLE_ADMIN') %}
            <div class="border-b pb-2 mb-1">
                <a href="{{ path('settings_system_information') }}">
                    {{ 'settings.system.settings'|trans }}
                </a>
            </div>
            <div class="border-b pb-2 mb-1">
                <a href="{{ path('settings_users_list') }}">
                    {{ 'settings.users'|trans }}
                </a>
            </div>
        {% endif %}
        <div>
            <a href="{{ path('settings_account') }}">
                {{ 'settings.account'|trans }}
            </a>
        </div>
    </div>
</div>
